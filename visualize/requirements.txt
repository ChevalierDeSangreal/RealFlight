numpy>=1.20.0
matplotlib>=3.3.0

# ROS2 相关包（需要先 source ROS2 环境）
# - rclpy (ROS2 Python API)
# - rosbag2_py (Bag 文件读取)
# - rosidl_runtime_py (消息反序列化)
# - px4_msgs (PX4 消息定义)
#
# 注意：这些包需要通过 ROS2 安装，不是 pip 包
# 运行前请先执行：
#   source /opt/ros/humble/setup.bash
#   source ./install/setup.bash

